# Markdown Linting

Config: `.markdownlint-cli2.jsonc`

Key rules:

- MD060: Table compact style — `| text |` only, no alignment padding; separators as `| --- |`
- MD040: Fenced code blocks must have a language (use `text` for plain output)
