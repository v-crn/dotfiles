# local files
**.local.**

# ZSH history
.zsh_history

# OS / editor artifacts
.DS_Store
*.swp
*.swo
*~

# chezmoi internal cache and local secrets
.chezmoicache/
chezmoi.toml
.chezmoisecrets*

# Dev environment artifacts (sandbox / IDE)
/.idea
/.vscode
/.mcp.json

# Dotfiles that belong in home/ (not repo root)
# These may appear as sandbox device files in some environments
/.bash_profile
/.bashrc
/.gitconfig
/.gitmodules
/.profile
/.ripgreprc
/.zprofile
/.zshrc
